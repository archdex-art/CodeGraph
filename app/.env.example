# CodeGraph product app configuration.
# No required secrets for local use — the backend clones public repos via git
# and stores results in ./data/codegraph.sqlite (created automatically).

# Optional: where the marketing site should link for "Start Indexing".
# Set this in the WEBSITE's env (CodeGraph/website/.env.local), not here:
#   NEXT_PUBLIC_APP_URL=http://localhost:4000
