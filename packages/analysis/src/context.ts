/** Moved to `@codegraph/analysis-model` (LLD §13): every extracted stage needs this seam.
 * Re-exported so existing importers keep working. */
export { throwIfAborted } from "@codegraph/analysis-model";
export type { PipelineContext } from "@codegraph/analysis-model";
