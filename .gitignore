# Dependencies
node_modules/
**/node_modules/

# Next.js build output
.next/
out/
next-env.d.ts
*.tsbuildinfo

# App runtime data (SQLite DB lives on the Render disk, not in git)
apps/web/data/*.sqlite
apps/web/data/*.sqlite-shm
apps/web/data/*.sqlite-wal

# Built-in editor: persistent per-repo git workspaces (cloned target repos —
# runtime state on the Render disk, never part of this repo's own history)
apps/web/data/workspaces/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Env & secrets
.env
.env*.local

# Misc
.DS_Store
*.log
